int x = 0;
int y = 0;
int taille = 40;
void setup() {
  size(400,400);
  
  square(x,y,taille);
  x = x + taille;
  y = y + taille;
  square(x,y,taille);
  x = x + taille;
  y = y + taille;
  square(x,y,taille);
  x = x + taille;
  y = y + taille;
  square(x,y,taille);
  x = x + taille;
  y = y + taille;
  square(x,y,taille);
  x = x + taille;
  y = y + taille;
  square(x,y,taille);
}
